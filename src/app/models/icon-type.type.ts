export type IconType = 'Message' | 'Phone' | 'Coffee' | 'Beer' | 'Meeting Note';

export enum TypeMessageEnum {
  'Message' = 'added a note to',
  'Phone' = 'had a call with',
  'Coffee' = 'had a coffee with',
  'Beer' = 'won\'t remember a night with',
  'Meeting Note' = 'had a meeting with'
}
